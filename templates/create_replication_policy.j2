{
  "copy_by_chunk": {{ item.copy_by_chunk | default(true) }},
  "dest_namespace_replace_count": 1,
  "name": "{{ item.policy_name }}",
  "description": "{{ item.description | default('') }}",
  "enabled": {{ item.enabled | default(true) }},
  "override": {{ item.override | default(true) }},
  "speed": 0,
  "src_registry": {
    "id": {{ item.registry_id }},
    "insecure": {{ item.insecure | default(true) }},
    "name": "{{ item.source_registry_name | default('') }}"
  },
  "trigger": {
    "trigger_settings": {},
    "type": "{{ item.trigger_type | default('manual') }}"
  },
  "deletion": {{ item.deletion | default(true) }}
}