{
  "username": "{{ item.username }}",
  "email": "{{ item.email }}",
  "password": "{{ item.password | default("MyFirstPassword100") }}",
  "realname": "{{ item.realname | default(item.username) }}",
  "comment": "{{ item.comment | default('') }}",
  "deleted": false,
  "role_name": "{{ item.role_name | default("developer") }}",
  "role_id": {{ item.role_id | default(2) }},
  "has_admin_role": {{ item.has_admin_role | default(true) }}
}
